//use super::*;
//extern crate htmlwalk;
#[cfg(test)]
mod htmlwalk_lib_tests {
    //use super::*;
    //use crate::lib::walk;
    //use lib::walk;
    //use once_cell::sync::Lazy;

    // maintain my nice tidy test structs
    //#[rustfmt::skip]

    #[test]
    fn how_do_we_test_this_when_we_return_nothing() {
        #[rustfmt::skip]
        assert_eq!( 1, 1 );
    }
}
